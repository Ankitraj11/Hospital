<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hospital">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PatientDescriptionActivity"></activity>
        <activity android:name=".DoctorDescriptionActivity" />
        <activity android:name=".UpdateProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MyAppointmentActivity" />
        <activity android:name=".MeetingChatActivity" />
        <activity android:name=".MedicineDescActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CommentActivity" />
        <activity android:name=".AppointmentRequestActivity" />
        <activity android:name=".AppointmentDescActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>